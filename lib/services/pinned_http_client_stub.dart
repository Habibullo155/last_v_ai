import 'package:http/http.dart' as http;

/// На вебе dart:io недоступен вообще (не просто "не работает", компилятор
/// не находит эти типы при сборке под веб) - TLS/сертификаты там
/// полностью управляются самим браузером, у кода на Dart нет доступа к
/// сертификату сервера на таком низком уровне. Pinning этим механизмом
/// на вебе невозможен в принципе - обычный клиент, обычная проверка
/// через доверенные CA браузера (та же защита, что у любого сайта).
const List<String> pinnedCertificateSha256Fingerprints = [];

http.Client createHttpClient() => http.Client();
